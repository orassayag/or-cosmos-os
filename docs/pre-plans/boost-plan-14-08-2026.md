### ⚠️ Issues Found (6): 1 🔴 | 4 🟡 | 1 🔵

| # | Category | Severity | Conf | Title | Detected by | Description |
|---|----------|----------|------|-------|-------------|-------------|
| I1 | Security | 🔴 High | C3 | The AI "applier" can write to any file in the repo, not just the files it's supposed to touch | boost | The nightly pipeline lets an AI edit the Cosmos data files and then opens a pull request with the result. It's meant to only ever change a small, named set of "writable" files (`src/scenarios/…` and `drift-sync/cosmos-confirmed.json`). But the actual guard is much looser than intended. `toolWrite` in `drift-sync/scripts/lib/agent.ts:81` only checks that the path stays *inside the repo root* — it does **not** restrict writes to that named set. Meanwhile the system prompt tells the AI "if you accidentally try to write outside the writable surface, write_file will reject with an error" (`drift-sync/scripts/apply-edits.ts:143`) — that promise is false; the write actually succeeds. The safety net that cleans up afterwards only covers the narrow set too: the pre-run cleanliness check (`apply-edits.ts:91`), the dry-run revert (`apply-edits.ts:239`), and the per-team reset (`sync-nightly.ts:222`) all scope their `git checkout` / `git status` to `WRITABLE_PATHS`. So if the AI writes to, say, `src/App.tsx` or `package.json`, that edit is **not** rejected, **not** reverted in a dry run, and **not** detected by the "is the tree clean?" check before the next team's run — it silently lingers in the working tree for the rest of the nightly run. |
| I2 | Bug | 🟡 Medium | C3 | The `investigate-topic` command looks for the API key in the wrong folder (duplicated env-loader that drifted) | boost | Loading the Anthropic API key from a `.env` / `.env.local` file is done in two places that are near-identical copies of each other. The shared helper `loadEnv()` (`drift-sync/scripts/lib/agent.ts:17`) — used by `diff-repo`, `apply-edits`, and the nightly run — reads the file from the **repo root** (`path.resolve(here, '..', '..', '..')`). But `drift-sync/scripts/sync.ts:28-49` inlines its **own** copy of that same parsing loop and reads from **`drift-sync/`** instead (`path.resolve(here, '..')`). Concrete effect: a developer following the project's own convention puts `.env.local` at the repo root (that's where the Vite app and every other script read it). Then `npm run sync -- investigate-topic <id>` silently fails to find the key, constructs `new Anthropic()` with nothing set, and the first API call dies with an authentication error — even though every other command works fine. The two copies also duplicate ~20 lines of parsing logic. |
| I3 | Bug | 🟡 Medium | C3 | The published model prices are wrong, so the "cost of this run" report over-states spend by up to 3× | boost | `MODEL_PRICING` in `drift-sync/scripts/lib/agent.ts:312` hard-codes per-million-token prices used to print an estimated dollar cost in the GitHub run summary. Two rows are stale: `claude-opus-4-7` is listed at `input: 15, output: 75`, but the current published rate is **$5 / $25** — a 3× overstatement. `claude-haiku-4-5` is listed at `input: 0.80, output: 4`, but the current rate is **$1 / $5**. (`claude-sonnet-4-6` at $3/$15 is correct.) Anyone reading the "Est. cost this run" line to gauge spend gets a misleadingly high number for Opus-driven runs. |
| I4 | Bug | 🟡 Medium | C2 | The validator finds its config file using a URL path, which breaks if the project folder has a space or special character in its name | boost | Every other file in the project derives its own directory the safe, cross-platform way with `fileURLToPath(import.meta.url)`. One file does not: `drift-sync/scripts/validate.ts:60` uses `path.dirname(new URL(import.meta.url).pathname)` to locate `cosmos-confirmed.json`. The `.pathname` of a file URL is **percent-encoded** — a folder named `My Repo` becomes `My%20Repo`. On such a path `existsSync`/`readFileSync` look for a literally-`%20` path that doesn't exist, so the validator silently skips all confirmed-suppression entries and re-reports warnings the team already signed off on. (The rest of the codebase's consistent use of `fileURLToPath` is what makes this one line stand out — see the grep at `validate.ts` vs. `diff-repo.ts:70`, `apply-edits.ts:53`, etc.) |
| I5 | DX | 🟡 Medium | C3 | There are no automated tests at all, and no test runner, for logic that is easy to test and easy to get wrong | boost | `scope:repo` — `package.json` has no `test` script and no test runner (vitest/jest) installed, and there are zero `*.test.ts` files. Several pieces of this codebase are pure, deterministic functions that are both high-consequence and cheap to test: the prefilter regexes and `prefilterDecision` (`drift-sync/scripts/lib/prefilter.ts`), the config-precedence resolver `loadDriftSyncConfig` (`lib/config.ts:98`), the owner resolver `resolveOwner` (`src/scenarios/owners.ts:64`), the cost math `estimateCost`/`sumUsage` (`lib/agent.ts`), and the git helpers. A single wrong character in a prefilter regex would silently let real drift slip through (the module's own comment says false negatives are "worse"), and nothing would catch it. |
| I6 | Bug | 🔵 Low | C3 | Several errors are silently swallowed with empty `catch { }` blocks | boost | The project's own error-handling guideline says never swallow an exception without logging or wrapping it. `sync-nightly.ts` has ~13 `catch { /* ignore */ }` blocks (e.g. `sync-nightly.ts:223-227`, `:328`, `:330`, `:807`, `:862`). Most sit around best-effort git cleanup where continuing is reasonable — but because they emit nothing, a genuinely broken git state (e.g. `git checkout` failing on a locked index) disappears with no trace, and the run proceeds as if the tree were clean. At minimum these should log at debug/warn level so a failed cleanup is diagnosable from the run output. |

_Note: `eslint`, `tsc --noEmit`, and `jsinspect` (duplicate detection) could not be run in this session (command execution was blocked), so the cosmetic/lint pass is limited to manual inspection. Manual scans found no `TODO`/`FIXME`, no `any` usage, no `console.*` in `src/`, and no trailing whitespace — the code is clean by eye. Re-run `npm run lint` and `npm run build` locally to confirm._

Also suggest the most cheep features to add and have the biggest affects on the project from all the suggestions.

### 🛠️ Recommendations

| # | Severity | How to Fix |
|---|----------|------------|
| R1 | 🔴 High | Make the write sandbox match the declared writable surface. In `toolWrite` (`lib/agent.ts:81`), after resolving the path, also assert it starts with one of the `WRITABLE_PATHS` prefixes (pass the allow-list into `createDispatcher`/`toolWrite` as an argument), and return an error otherwise — e.g. `if (!WRITABLE_PATHS.some(w => rel === w || rel.startsWith(w))) return 'ERROR: write outside writable surface: ' + rel;`. That makes the system prompt's promise ("write_file will reject") actually true, and keeps a misbehaving applier from touching anything but the intended data files. As defense-in-depth, widen the dry-run revert and the pre-run cleanliness check to the whole tree (`git status --porcelain` with no pathspec) so any stray write is at least detected. |
| R2 | 🟡 Medium | Delete the inlined env block in `sync.ts:24-49` and `import { loadEnv } from './lib/agent.js'`, then call `loadEnv()` once near the top (as `apply-edits.ts` and `diff-repo.ts` already do). This removes the duplicated ~20 lines **and** fixes the wrong-directory bug in one move, because the shared helper already resolves the repo root correctly (three levels up). Verify with a repo-root `.env.local` that `npm run sync -- investigate-topic <id>` now authenticates. |
| R3 | 🟡 Medium | Update the two stale rows in `MODEL_PRICING` (`lib/agent.ts:313-315`) to the current published rates: `'claude-opus-4-7': { input: 5, output: 25, cacheWrite: 6.25, cacheRead: 0.50 }` and `'claude-haiku-4-5': { input: 1, output: 5, cacheWrite: 1.25, cacheRead: 0.10 }`. Add a short comment pointing at the pricing page and the date checked, and consider a tiny unit test asserting a known token count → known dollar figure so a future price drift is caught. |
| R4 | 🟡 Medium | Replace `path.dirname(new URL(import.meta.url).pathname)` at `validate.ts:60` with the same idiom the rest of the repo uses: `import { fileURLToPath } from 'node:url'` then `path.dirname(fileURLToPath(import.meta.url))`. This decodes the path correctly regardless of spaces or special characters, and makes the file consistent with its siblings. |
| R5 | 🟡 Medium | Add a lightweight test runner — `vitest` fits a Vite/TS project with near-zero config — wire up `"test": "vitest run"` in `package.json`, and start with the pure functions where a bug is silent and costly: table-driven tests for `prefilterDecision` (a handful of representative diffs → expected skip/keep), `loadDriftSyncConfig` precedence (env > file > default), `resolveOwner` (override/team/fallback branches), and `estimateCost`. Follow the project's `__tests__/<name>.test.ts` convention. |
| R6 | 🔵 Low | In each swallowed `catch` in `sync-nightly.ts`, log the error before ignoring it — e.g. `catch (err) { console.log('  (warn) cleanup step failed: ' + String(err).slice(0, 160)); }`. Keep the "continue anyway" behaviour for best-effort cleanup, but make the failure visible in the run log so a wedged git state is diagnosable instead of invisible. |

### Overall Verdict

🟡 Healthy foundation — targeted improvements recommended.
